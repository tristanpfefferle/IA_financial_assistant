# IA Financial Assistant (Architecture Skeleton)

Architecture production minimale avec séparation stricte:
`backend/`, `agent/`, `ai/`, `ui/`, `shared/`, `infra/`, `docs/`.

Voir `docs/architecture.md` et `AGENTS.md`.
